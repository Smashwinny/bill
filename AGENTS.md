# 项目永久交付规则

以下规则适用于本项目中的所有开发、修改、修复、配置和文档工作。

## 1. Git 留痕

- 当前受控工作区的 `.git` 是不可写的空挂载点，仓库元数据存放在项目根目录 `.git-repo`；在该环境中使用 `git --git-dir=.git-repo --work-tree=.` 执行 Git 命令。
- 任何对项目文件的修改都必须纳入 Git 版本管理并创建提交，不允许只修改工作区而不提交。
- 每个提交应只包含一个边界清晰、可解释、可验证的工作单元。
- 提交信息必须准确描述本次变更，禁止使用含义不明的提交信息。
- 不得通过改写、压缩或删除历史来掩盖开发过程；未经用户明确许可，不得执行强制推送、变基或破坏性历史操作。
- 提交前必须检查差异，避免纳入无关文件、密钥、账号、签名材料或本机临时文件。
- 交付时必须报告 Git 提交哈希；正常情况下，交付完成后的工作区应当干净。

## 2. 可验证交付

- 每次交付都必须提供可重复执行的验证方法，说明验证前提、命令或手机操作步骤、预期结果。
- 必须明确指出所有交付产物的位置，使用项目内路径或绝对路径，不能只说“已经生成”。
- Android 功能性变更应优先生成可直接安装到手机验证的 APK。
- APK 交付必须说明：APK 路径、构建变体、安装命令或安装方法、验证步骤及预期现象。
- 如果受环境、依赖、签名、设备权限或其他条件限制而无法生成 APK，必须明确说明阻碍、已经完成的替代验证以及用户如何补充验证；不得把未验证产物描述为已验证。
- 纯文档、设计或不适合生成 APK 的变更，可以交付文件、测试报告、命令输出或其他与变更相匹配的可验证证据。

## 3. Android 默认验证基线

- 开发阶段默认至少执行与改动相关的自动化测试或静态检查。
- 可以构建时，默认生成 Debug APK，便于用户直接侧载验证。
- APK 安装前后不得暗中清除用户已有应用数据；需要卸载、清数据或迁移数据库时，必须提前明确说明。
- 涉及通知监听、后台运行、支付解析、退款识别或厂商保活策略的功能，必须给出真机验证清单，不能只以模拟器或单元测试结果代替真机结论。

## 4. 每次交付报告格式

交付说明至少包含：

1. 本次完成内容。
2. Git 提交哈希和提交信息。
3. 产物的准确位置。
4. 已执行的验证及结果。
5. 用户可重复执行的验证步骤和预期结果。
6. 尚未验证的部分、已知限制或风险。
