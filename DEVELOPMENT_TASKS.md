# 自动记账应用：可验证的小任务清单

## 1. 使用方法

本清单以“每个任务都能产生独立、可验证结果”为拆分标准，适合一次只交给一个小模型执行。

执行规则：

1. 严格按编号顺序执行；标记为阻断门的任务未通过时，不得进入后续任务。
2. 一个任务对应一个 Git 提交，不顺带实现下一个任务。
3. 每次开始前检查工作区，结束时提交并确认工作区干净。
4. 从 T01 起，功能任务默认交付 Debug APK；仅文档任务可以不生成 APK。
5. 自动化测试证明代码逻辑，真机测试证明 Android 系统行为，两者不能互相替代。
6. 真机支付和退款必须由用户本人操作；程序和开发代理不得自动发起真实交易。
7. 涉及通知原文的日志、截图和导出文件默认视为敏感数据，不得提交到 Git 或上传到第三方。

统一 APK 预期位置：

```text
app/build/outputs/apk/debug/app-debug.apk
```

如果具体构建结构导致路径不同，交付时必须给出实际绝对路径。

## 2. 完成定义

每个任务只有同时满足以下条件才算完成：

- 范围内功能已实现，范围外功能没有提前混入。
- 相关自动化测试或静态检查通过。
- APK 已构建；若任务要求真机，则已给出真机验证步骤和结果记录入口。
- 文档、代码和实际行为一致。
- 已创建 Git 提交，并报告完整提交哈希。
- 已列出产物位置、验证命令、预期结果、未验证项和风险。
- `git status --short` 无输出。

## 3. 第一阶段：先验证最危险的数据入口

### T00 固化产品边界和数据可信度

目标：把“自动采集不等于权威账单”变成明确的产品规则，防止后续错误承诺。

修改范围：只新增设计文档，不创建 Android 工程。

产物：

- 支持和不支持的数据来源列表。
- `DETECTED`、`SUCCESS`、`PARTIALLY_REFUNDED`、`FULLY_REFUNDED`、`REVERSED` 状态定义。
- 自动确认、待确认、已对账三级可信度定义。
- 隐私边界和日志脱敏规则。

验证：人工检查每一种状态都有进入条件、退出条件和无法判断时的处理方式。

阻断条件：如果用户要求仅凭通知实现绝对零漏记、零多记，则停止实现并重新确认目标。

### T01 创建最小可安装 Android 工程

目标：只建立能够安装启动的 Kotlin/Compose 工程。

功能：显示应用名称、版本号、构建时间和“工程运行正常”。

不做：数据库、通知权限、记账页面、联网。

自动验证：执行单元测试、Lint 和 Debug 构建。

真机验证：安装 APK，冷启动成功，退出后再次启动不崩溃。

产物：Debug APK。

### T02 通知监听权限与连接状态

目标：接入 `NotificationListenerService`，但暂不解析支付内容。

功能：

- 显示通知读取权限是否开启。
- 一键跳转系统通知读取授权页。
- 显示监听服务最近连接、断开时间。
- 不申请辅助功能、短信、通讯录、存储或悬浮窗权限。

自动验证：服务声明和状态映射测试通过。

真机验证：授权前显示未开启；授权后服务显示已连接；取消授权后状态恢复未开启。

产物：Debug APK和权限验证截图位置说明。

### T03 原始通知事件本地采集

目标：证明系统能在应用不处于前台时交付通知事件。

功能：

- 只采集用户明确启用的包名。
- 保存通知 Key、包名、发布时间、接收时间、标题、正文和内容哈希。
- 提供本地事件列表和“清空测试数据”按钮。
- 禁止联网，禁止向日志打印完整通知正文。

自动验证：相同事件写入两次时，数据库只保留一条。

真机验证：将 App 切到后台，由一个普通测试 App 发通知；返回后能看到一条原始事件。

产物：Debug APK。

### T04 支付渠道真机可行性探针（第一阻断门）

目标：用真实设备确认目标支付 App 实际提供了哪些信号，不做通用猜测。

范围：微信、支付宝和用户实际使用的银行 App 分别测试，不实现正式账目。

每个渠道至少记录：

- 成功支付是否产生通知。
- App 在前台、后台以及锁屏时是否产生通知。
- 通知是否含金额、商户、订单号和成功状态。
- 全额退款是否产生新通知。
- 部分退款是否产生新通知。
- 通知更新是否复用了相同 Key。
- 关闭该渠道通知后，应用能否明确提示数据不完整。

验证：用户使用本人账户完成获授权的小额测试交易和退款，然后将 App 内脱敏探针报告与渠道账单人工对比。

产物：Debug APK，以及不包含账号、完整订单号和通知原文的测试报告。

通过标准：至少一个目标渠道的成功支付能够稳定产生足以识别金额和状态的事件。

失败处理：停止后续自动解析，转向账单导入、分享到账本或银行流水方案。

## 4. 第二阶段：把通知安全地变成交易候选

### T05 建立 RawEvent 数据库和诊断导出

目标：正式保存原始事件，并支持脱敏诊断。

功能：Room 数据库、Schema 导出、迁移测试、原始事件分页；诊断导出默认掩码金额之外的个人信息和订单号。

验证：重启 App 后事件仍存在；数据库迁移测试通过；导出文件中不存在配置的敏感样例。

产物：Debug APK、Room Schema、脱敏样例报告。

### T06 可测试的解析器框架

目标：把通知解析从 Android 服务中分离为纯 Kotlin 规则引擎。

功能：输入标准化 RawEvent，输出“无法识别”或结构化候选；规则带渠道和版本号。

验证：使用脱敏固定样例完成成功、失败、格式变化、缺失金额和异常字符测试；未知格式必须返回待确认，不能猜测成功。

产物：Debug APK和解析测试报告。

### T07 实现第一个支付渠道解析器

目标：只实现 T04 中证据最充分的一个渠道。

功能：识别成功支付金额、时间、商户和可用订单标识；无法确认时进入待确认。

验证：固定样例全通过；用新的真机小额交易验证一次，解析结果与账单一致。

产物：Debug APK和脱敏验证记录。

### T08 幂等写入和重复通知合并

目标：相同事件重复投递时只产生一个交易候选。

规则：优先使用渠道订单标识；没有订单标识时使用来源、金额、标准化商户、时间窗和事件哈希，模糊匹配不得静默删除候选。

验证：覆盖同 Key 重放、不同 Key 同订单、相同金额不同商户、短时间连续同额消费和并发写入测试。

产物：Debug APK和去重测试报告。

### T09 支付成功候选确认界面

目标：让用户看到系统为什么创建了一笔账，而不是在后台静默记账。

功能：候选列表、来源、金额、商户、时间、可信度、确认、修正和忽略；保留候选到原始事件的追溯关系。

验证：从测试通知生成候选，确认后进入正式账目；忽略后不计入统计；修正保留修改记录。

产物：Debug APK。

## 5. 第三阶段：退款与账目完整性

### T10 交易状态机与审计记录

目标：建立正式交易、状态变化和不可覆盖的审计记录。

验证：非法状态跳转被拒绝；每次修改都保留修改前后值、时间和来源；单元测试覆盖全部允许路径。

产物：Debug APK和状态机测试报告。

### T11 第一个渠道的全额退款解析

目标：把退款保存为独立事件并关联原支付，不把退款伪装成普通收入。

验证：支付后全额退款，原交易变为 `FULLY_REFUNDED`，净支出为零，原支付和退款记录都可查看。

产物：Debug APK和真机脱敏验证记录。

### T12 部分退款和多次退款

目标：支持一笔支付对应一次或多次部分退款。

验证：退款累计小于原金额时为 `PARTIALLY_REFUNDED`；等于原金额时为 `FULLY_REFUNDED`；超过原金额时禁止自动确认并进入异常队列。

产物：Debug APK和状态机测试报告。

### T13 无法关联退款的异常队列

目标：退款找不到原交易时不丢弃、不错误创建普通收入。

功能：显示候选原交易、人工关联、新建历史原交易和暂不处理。

验证：构造同金额多笔支付、跨月退款和原交易缺失场景，均不得自动误关联。

产物：Debug APK。

### T14 账单导入与对账（第二阻断门）

目标：使用目标渠道真实可导出的账单格式，发现通知漏记、多记和状态错误。

先决条件：先取得用户本人导出的脱敏账单样例，再设计导入器；不得凭空假设 CSV 列。

功能：导入预览、格式识别、重复导入幂等、差异列表；导入前不改正式账目，用户确认后才应用。

验证：同一文件导入两次不重复；能够列出“仅通知存在”“仅账单存在”“金额或状态不一致”。

产物：Debug APK和脱敏账单夹具。

通过标准：一次完整测试周期内，账单中的测试支付及退款最终都能在 App 中得到解释。

## 6. 第四阶段：成为可日常使用的记账应用

### T15 正式账目列表和详情

目标：按日期查看支付、退款、净金额、来源和对账状态。

验证：跨日、跨月、支付加退款和大量数据分页显示正确。

产物：Debug APK。

### T16 分类和人工修正

目标：设置餐饮、交通等分类，并允许修正商户和用途。

约束：修改不能破坏原始事件、原金额、退款关联和审计记录。

验证：分类修改立即反映在详情中，重启后保留，审计记录可追溯。

产物：Debug APK。

### T17 搜索、筛选和待处理入口

目标：按日期、渠道、分类、金额、商户、退款和对账状态筛选。

验证：预置数据集的每种筛选结果数量与测试期望一致。

产物：Debug APK。

### T18 统计

目标：提供日、月、分类和支付渠道统计。

约束：同时展示原始支出、退款和净支出；待确认交易默认不计入正式统计。

验证：使用固定数据集人工计算期望值并与界面、单元测试逐项比较。

产物：Debug APK和统计测试报告。

### T19 采集健康中心

目标：用户随时知道自动采集是否可能不完整。

功能：通知权限、服务连接状态、目标渠道通知设置、最近事件时间、未处理数量和未对账数量；异常时显著提示。

验证：依次撤销通知权限、关闭渠道通知、重启手机和限制后台运行，记录每种状态能否被识别；无法由 Android 可靠识别的厂商限制必须明确标为未知。

产物：Debug APK和真机测试矩阵。

### T20 重启与低功耗可靠性测试

目标：证明方案是事件驱动，不依赖高耗电常驻轮询。

验证周期：至少连续运行 72 小时，包含重启、锁屏、长时间后台和多次真实通知；记录电池用量、监听断线、漏收和重复数。

产物：同一候选版本 APK和72小时测试报告；该任务可以只有测试与报告提交，不强制修改业务代码。

通过标准：测试数据达到用户确认的标准后才能称为“可日常试用”；未达标则创建针对性修复任务，不直接进入同步开发。

## 7. 第五阶段：备份、迁移和远端同步

### T21 本地完整备份导出

目标：导出带格式版本、校验值和生成时间的完整 JSON 备份。

验证：备份中包含交易、退款、分类、审计关系和必要原始事件；损坏文件能被拒绝。

产物：Debug APK和不含真实隐私数据的样例备份。

### T22 恢复预览和新设备迁移

目标：导入前显示新增、重复、冲突和忽略数量，用户确认后才写入。

验证：在空数据库恢复后统计完全一致；重复恢复不产生重复；旧数据不能被意外覆盖。

产物：Debug APK和往返一致性测试报告。

### T23 数据模型同步准备

目标：为每个可同步实体加入 UUID、创建时间、修改时间、删除墓碑、版本和设备标识，不接服务器。

验证：离线修改、删除、重复应用同一变更和时钟相同场景有确定结果；数据库迁移不丢数据。

产物：Debug APK和迁移测试报告。

### T24 远端接口方案确认

目标：在自建服务、WebDAV、Supabase 或其他方案中选定一种，并完成威胁模型、认证、加密和冲突策略文档。

这是设计任务，不写同步业务代码。用户确认方案后才能继续。

验证：文档必须回答服务端能看到什么、密钥存放在哪里、设备丢失怎么办、冲突如何处理、如何导出和删除远端数据。

### T25 单用户加密同步最小闭环

目标：只实现上传、另一设备下载、离线修改后重试和幂等，不做多用户分享。

验证：两台设备完成新增、修改、删除、退款关联和断网恢复测试；服务端重复请求不产生重复数据。

产物：Debug APK、服务端版本或部署产物、双设备测试报告。

## 8. 后续渠道扩展方式

每增加一个支付或银行渠道，固定拆成三个独立任务：

1. 真机探针：确认成功支付和退款通知格式，形成脱敏证据。
2. 解析器：用固定样例实现并完成自动化测试。
3. 对账器：用该渠道实际导出格式验证漏记、多记和退款状态。

不允许只根据网络截图、旧版本通知或其他手机型号的表现，直接声称新渠道已经支持。

## 9. 可直接交给小模型的任务指令模板

```text
只执行 DEVELOPMENT_TASKS.md 中的任务 Txx，不实现任何后续任务。
开始前完整阅读 AGENTS.md 和该任务的目标、范围、验证与阻断条件。
先检查当前 Git 状态和已有实现；保留所有无关修改。
完成后执行规定的测试，构建 Debug APK，并创建一个边界清晰的 Git 提交。
最终报告必须包含：完成内容、完整提交哈希、APK 或文档的绝对路径、已执行验证及结果、用户真机验证步骤、预期结果、未验证项和风险。
若缺少真机、真实通知或账单样例，只完成可在当前环境验证的部分并明确停在等待验证状态，不得臆造测试结果或越过阻断门。
本环境使用 git --git-dir=.git-repo --work-tree=. 执行 Git 命令。
```

