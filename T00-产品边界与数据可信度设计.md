# T00 产品边界与数据可信度设计（独立文档）

## 1. 目标

该文档仅用于 T00 任务范围：
- 固化产品边界与可信边界。
- 明确通知数据的状态模型与证据规则。
- 明确自动确认、待确认、已对账三级可信度。
- 明确隐私与数据处理边界。
- 不承诺“零漏记/零多记”，并定义与正式账单冲突的处理规则。

## 2. 数据来源能力与限制

### 2.1 支持的数据来源（当前设计内）

1. 通知事件（主信号）
- 来源：系统通知监听。
- 可提供：到达时间、通知时间、应用包名、标题/正文文本（仅在本地暂存）、事件指纹。
- 用途：生成原始候选、触发状态识别、辅助匹配退款/回退。

2. 用户手工确认（主校验入口）
- 来源：用户在界面中的确认、修正、忽略、关联。
- 用途：把机器可疑识别转为用户可解释的账目动作。

3. 账单/对账输入（T00 定义的协同约束）
- 来源：用户导入或后续任务中确认的脱敏账单。
- 用途：核对通知事件完整性与金额/状态一致性，产生例外/差异。

### 2.2 明确的能力边界与限制

- 通知不是权威账本。无法保证事件一定产生、及时产生或完整产生。
- 通知文本可能缺失或缺字段（尤其订单号、金额、完整状态），需退到待确认，不得猜测。
- 同一事件可能重复推送、顺序错乱、文本变化。
- 系统权限变化（通知权限、系统策略、厂商限制）会导致事件缺失。
- 不同应用版本、系统版本导致消息格式变化，规则命中稳定性有限。
- 通知与真实账务仍可能出现差异；差异是系统设计内的已预期现象，不作“零漏记/零多记”承诺。

## 3. 状态模型

本模型用于交易候选/正式交易状态流转。每条状态均包含：
- `进入条件`
- `退出条件`
- `证据不足处理`

### 3.1 `DETECTED`

- 进入条件
  - 成功采集到一条新的通知事件；事件已解析到“可能支付相关”但尚未确认成功。
  - 或检测到与历史事件“疑似同一交易”但当前关键字段不完整。

- 退出条件
  - 解析后确认成功并具备可置信证据，进入 `SUCCESS`。
  - 解析判定为失败/非支付事件，可归档为非交易类处理或人工剔除。
  - 通知与对账任务关联后发现非目标交易证据，回退为待确认排查。

- 证据不足处理
  - 默认不入正式账目。
  - 保留原始事件与证据链（时间线、来源、字段缺失清单）。
  - 标记为“待确认”列表，可供用户处理。

### 3.2 `SUCCESS`

- 进入条件
  - 识别到“支付成功”语义并具备基础证据（来源、时间、金额、商户或订单关联）。
  - 可信度达到该任务约定阈值；或被用户手工确认。

- 退出条件
  - 收到同源退款事件（部分/全部）并满足对应条件，进入 `PARTIALLY_REFUNDED` 或 `FULLY_REFUNDED`。
  - 收到明确“冲正/撤销/失败覆盖”事件，进入 `REVERSED`。
  - 与账单对账后发现冲突且无法自动消歧，进入待确认/异常队列。

- 证据不足处理
  - 如果金额/商户/时间均不稳定，仅保留检测态，不升级为 `SUCCESS`。
  - 若存在“可能成功”但证据不全，强制标记待确认，必须用户或对账链路解锁。

### 3.3 `PARTIALLY_REFUNDED`

- 进入条件
  - 在 `SUCCESS` 基础上检测到一条退款事件且退款金额 `0 < refund_amount < paid_amount_remaining`，并与原交易可关联。
  - 若多条退款累计满足上述条件，状态保持 `PARTIALLY_REFUNDED`。

- 退出条件
  - 累计退款金额累计达到原支付金额后进入 `FULLY_REFUNDED`。
  - 新出现冲突事件（如重复全额冲正）或用户修正后，进入 `REVERSED` 或待确认。

- 证据不足处理
  - 退款金额、时间或关联字段不完整时，不得直接进入该状态。
  - 以“待确认/异常”保留并保留疑似关联候选，不自动改写原状态。

### 3.4 `FULLY_REFUNDED`

- 进入条件
  - 在 `SUCCESS` 基础上检测到退款累计金额等于原支付金额，且关联成功。
  - 或用户在手工核对后明确确认该支付已全额退款。

- 退出条件
  - 出现新的有效交易补充（同支付重算、重复导入差异澄清）导致金额关系变化，需进入待确认复核。
  - 收到反向冲正/账务修正且可验证，进入 `REVERSED`。

- 证据不足处理
  - 未能证明“关联关系唯一性”时，不更新状态。
  - 只展示可疑匹配列表和缺失字段；要求用户确认。

### 3.5 `REVERSED`

- 进入条件
  - 原交易状态被渠道明确标记为撤销、冲正、撤销成功、交易失败补偿完成。
  - 或对账发现该“成功”记录在官方账务链路被否决/回滚。

- 退出条件
  - 新证据推翻原冲正，恢复到 `SUCCESS`/`PARTIALLY_REFUNDED`/`FULLY_REFUNDED`（需审计记录）。
  - 明确用户确认后关闭该交易但保留历史变更。

- 证据不足处理
  - 若仅有“可能冲正”文字而无标准化字段，禁止自动进入该状态。
  - 进入待确认队列并要求人工确认是否真为冲正。

## 4. 三层可信度定义与转换规则

### 4.1 自动确认（Auto Confirmed）

- 条件
  - 解析规则稳定命中且字段完整。
  - 状态链路可自洽（如事件顺序、金额关系、时间窗）且无冲突。

- 约束
  - 仍默认低优先级于对账发现。
  - 出现与账单冲突时降级为待确认，不直接反向覆盖。

### 4.2 待确认（Pending Review）

- 条件
  - 字段缺失、文本歧义、重复冲突、跨渠道重复来源、金额对不上。
  - 人工未确认前全部进入待确认。

- 处理
  - 不自动计入正式财务统计。
  - 在产品界面中保留“证据缺口清单”：缺少字段、疑似重复、可疑匹配候选。

### 4.3 已对账（Reconciled）

- 条件
  - 与用户可验证的账单/官方渠道一致（金额、时间、状态、来源一致）。
  - 对账文件解析链路通过。

- 处理
  - 将该记录作为高可信事件，减少重复提示、异常警报。
  - 仍保留对账来源时间戳与来源文件/批次信息，便于追溯。

### 4.4 三层关系（必须执行）

- 自动确认不是最终真值，只是“系统可自信判定”。
- 待确认不是失败，只是“未满足入账阈值”。
- 已对账是高可信闭环，不替代对账外的用户修正权。

## 5. 仅凭通知不能承诺零漏记、零多记

- 系统只承诺“尽力采集+可追溯”，不承诺“绝对完整”。
- 风险场景：应用无通知、厂商限流、权限关闭、文本变更、重复推送。均可能导致漏记或多记。
- 产品文案/提示必须明确：
  - 通知数据是线索，不是发票式权威。
  - 若用户依赖强精度，应开启/使用后续对账功能并定期核对。

## 6. 通知与正式账单冲突处理规则（核心边界）

1. 冲突分级：
   - 无法验证冲突：维持原有状态，标记待确认。
   - 可验证金额/时间/状态冲突：以对账证据为优先，需用户确认后改写并保留审计。
   - 可验证“通知成功但账单不存在”
     - 标记“疑似漏计/误识别”。
     - 需要待确认，不自动入账。
   - 可验证“账单存在但通知未采集”
     - 标记“采集缺失风险”，纳入采集完整性告警。

2. 处理优先级
   - 证据完整且来源可靠的账单数据优于单条通知推断。
   - 任何冲突都保留原始事件与变更日志，禁止静默覆盖。

3. 决策原则
   - 不因为“更高看似权威的来源”直接清洗历史：必须可回溯到对应字段证据。
   - 冲突记录必须可解释：展示冲突字段、来源和时间。

## 7. 隐私边界

### 7.1 通知与日志边界

- 禁止采集/上报完整原文明文到第三方；仅保留本地必要字段和脱敏字段。
- 本地日志禁止记录完整通知正文、完整手机号、姓名、卡号、身份证号、订单号明文。
- 日志/崩溃信息中可记录 hash、长度、类型、时间戳，不记录敏感明文。

### 7.2 测试夹具边界

- 测试夹具必须脱敏：金额可保留但用可复核范围表示；商户名可脱敏后留语义类别。
- 禁止测试件中包含用户真实信息、完整订单号、姓名、手机号、证件号。

### 7.3 Git 交付边界

- 绝不提交原始通知正文、真实账单文件、真实截图、敏感日志样例。
- 仅提交规则文档、脱敏样例模板和测试脚本（若为纯文档任务，默认仅提交文档）。

### 7.4 未来同步边界（预定义）

- 同步仅处理加密后的非敏感聚合字段或已授权的脱敏字段。
- 不同步完整通知原文与可反推真实身份的字段。
- 同步通道必须记录：同步时间、来源设备、版本、校验摘要、变更来源。

## 8. 状态与证据不足输出规范

- 每条记录需支持三种展示：
  - `状态`
  - `当前可信度`
  - `未满足条件清单`（如金额缺失、订单号冲突、与账单未核对）

- 任何状态更新都要生成审计项：前置状态、后置状态、触发证据、操作者、时间。

## 9. T00 完成判定（与本任务一致）

- 已定义数据来源能力与限制。
- 已完整定义五个交易状态的进入/退出/证据不足处理。
- 已定义三层可信度及其转换逻辑。
- 已定义通知与账单冲突处理与“非零漏多记”边界。
- 已定义隐私边界（通知、日志、测试夹具、Git、未来同步）。
- 该文档为独立文件，不实现任何功能。
