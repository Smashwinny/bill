# T00 固化产品边界和数据可信度

**目标任务：** DEVELOPMENT_TASKS.md 的 T00

## 1. 支持与不支持的数据来源

### 支持的数据来源（仅作为“候选来源”）
- 已授权的支付/交易类应用通知（如微信、支付宝、招商银行卡、建设银行卡等）
- 用户在设置中手工开启的白名单应用通知
- 用户手工导入的脱敏账单（T14 之后才进入正式对账）
- 明确声明“可追踪交易变动”的系统事件（如用户触发的手动导入动作）

### 不支持的数据来源（不承诺覆盖）
- 语音播报、截图、短信摘要、剪贴板内容
- 应用内页面未发出的隐式支付结果（仅在页面停留、网页支付、第三方聚合场景）
- 只读日志或非用户开启权限的系统数据
- 未开启通知授权时的任何隐式推断
- 来自未知渠道、未白名单、无关键上下文字段的事件

### 不支持意味着什么
- 不保证“零漏记”或“零多记”
- 不承诺跨渠道、跨设备、跨账号的事件补齐
- 对于“无事件或事件缺失”不以默认成功状态记账

## 2. 状态定义与语义

### 2.1 事件接收/识别状态（系统内部）
- `DETECTED`
  - 表示：从监听到通知事件到达了可解析流水线
  - 触发条件：事件完整入库（至少有应用包名 + 时间 + 事件内容摘要）
- `SUCCESS`
  - 表示：事件被解析为“可计入交易候选”
  - 触发条件：金额、渠道、时间窗口、状态语义均可判定且不触发高风险矛盾规则
- `PARTIALLY_REFUNDED`
  - 表示：命中同一交易的退款事件，且累计退款额小于原交易金额
  - 触发条件：存在原交易关联 + 退款金额 > 0 + 累计退款额 < 原交易金额
- `FULLY_REFUNDED`
  - 表示：该交易已被退款且累计退款额等于原交易金额
  - 触发条件：存在原交易关联 + 累计退款额 = 原交易金额
- `REVERSED`
  - 表示：已解析或已确认事件被判定为“需回滚或异常恢复”
  - 触发条件：系统判定金额/渠道/订单上下文矛盾，或用户确认后否认该笔交易（保留审计）

### 2.2 可信度等级（产品层）
- 自动确认（CONFIRMED）
  - 规则：同一渠道内具备稳定关键字段（金额/商户/订单标识/时间/状态）且通过一致性校验
- 待确认（PENDING_REVIEW）
  - 规则：字段缺失、金额或商户置信不足、来源渠道更新频率低/波动大、通知字段冲突
- 已对账（RECONCILED）
  - 规则：与用户提供的账单（T14）进行比对后确认为同一交易且状态一致

## 3. 置信度状态转移（进入条件 / 退出条件 / 不可判断处理）

### 3.1 `DETECTED -> SUCCESS`
- 进入：
  - 解析器输出完整候选，且非冲突模板
- 退出：
  - 转为 `PENDING_REVIEW` 当金额/商户/订单存在歧义
  - 被系统归为重复事件并合并后，留存最新版本
- 无法判断处理：
  - 无法解析金额或状态时，不进入 `SUCCESS`，直接标记 `PENDING_REVIEW`

### 3.2 `DETECTED -> PENDING_REVIEW`
- 进入：
  - 缺失金额字段、模糊文案、渠道白名单外、时间异常（离散）
- 退出：
  - 用户手工确认后可进入 `SUCCESS` 或 `REVERSED`
  - 与可用账单对账后可进入 `RECONCILED`

### 3.3 `SUCCESS -> FULLY_REFUNDED / PARTIALLY_REFUNDED`
- 进入：
  - 识别到对应退款事件且完成交易关联
- 退出：
  - 用户后验手工修正后可退回 `PENDING_REVIEW`（保留原状态审计）
  - 退款金额异常高于原交易时，进入 `PENDING_REVIEW` 并标记异常

### 3.4 `REVERSED`
- 进入：
  - 用户确认误记账
  - 自动校验检测到明显冲突且无法消歧
- 退出：
  - 仅可由审计流程或人工修正恢复到可核验状态（通常是 `PENDING_REVIEW`）
- 风险约束：
  - 不能直接被新事件覆盖，必须保留原始事件和撤销原因

## 4. 数据完整性与不可承诺边界
- 未开启通知权限时：系统只保留用户手工导入数据，不从监听采集新事件
- 通知权限开启但未授权目标渠道：该渠道显示“数据不完整”而非自动降级为成功入账
- 采集到的事件会保留“原始文本摘要哈希 + 去标识化主键 + 时间”
- 所有规则均不承诺与银行/支付方实时对账一致，支付完成不代表已可记账

## 5. 隐私边界与日志脱敏规则
- 采集与存储
  - 不采集完整账号、支付密码、短信验证码、完整订单号
  - 通知原文默认仅保存哈希或脱敏片段；原文仅在最短时间窗口内进入临时处理流
  - 存储字段以“最小可识别集合”为原则：`packageName / title / contentHash / eventTime / receiveTime / status`
- 日志规则（默认不落盘明文）
  - 允许日志：事件级别、状态流转、来源包名（非敏感字段）
  - 禁止日志：完整通知正文、用户姓名、电话、卡号后 4 位以上、订单号完整串
  - 风险告警日志需记录 `event_hash`、时间、状态、失败原因，不能记录原始通知内容
- 导出与共享
  - 导出文件默认移除/遮罩金额以外的金额字段外，必须对文本类字段执行掩码
  - 默认屏蔽完整时间戳序列重建用户行为路径的字段组合
- 数据保留
  - 原始事件保留期和清理机制由后续任务逐步细化；T00 只定义“隐私最小化先行、保留周期可配置”

## 6. 阻断条件说明（T00 对齐）
- 若出现“仅凭通知实现绝对零漏记、零多记”的诉求：不进入后续自动解析/对账任务，需先重新确认产品目标

## 7. 验收要点（人工可验）
- 每一种状态都可追溯“进入条件 + 退出条件 + 不可判断处理”：
  - `DETECTED`
  - `SUCCESS`
  - `PARTIALLY_REFUNDED`
  - `FULLY_REFUNDED`
  - `REVERSED`
- 每一种可信度等级都具备明确边界与迁移路径：
  - 自动确认
  - 待确认
  - 已对账
- 隐私边界明确到字段级，不出现“按渠道自动绝对准确”的承诺语义
