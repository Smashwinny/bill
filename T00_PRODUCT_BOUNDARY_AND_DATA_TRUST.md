# T00 任务设计文档（执行边界与可信度）

本文件仅覆盖 **DEVELOPMENT_TASKS.md** 的 **T00**，不实现任何 Android 代码与后续任务。

## 1) 数据来源边界

- 通知监听可以发现什么
  - 已授权且在白名单内的应用推送事件（支付、消费、退款相关通知）
  - 可观测字段：通知来源 `packageName`、时间、标题、文本片段（哈希/脱敏），以及系统层级事件元数据
  - 可确认“有无新通知到达”与“到达时序”

- 账单导入可以确认什么
  - 可核验的历史交易列表（金额、交易时间、状态、支付方/渠道、可关联标识）
  - 可用于做“去重、异常、状态矫正、漏记/多记比对”
  - **可确认**：同一交易是否在正式账单里存在、交易状态是否一致
  - **不直接代表**：实时支付成功，更多是对照与纠偏依据

- 哪些情况下可能漏记
  - 通知权限关闭、目标应用未授权通知读取、通道未在白名单
  - 应用未发通知（或仅更新未发系统通知）
  - 系统限流、系统休眠、厂商策略导致的通知丢失
  - 锁屏/系统策略导致的跨应用通知聚合变形

- 哪些情况下可能重复
  - 同一交易由多个应用/渠道重复推送（如支付应用 + 银行 App）
  - 同一消息多次重投递（同 key 或同内容变体）
  - 退款/成功消息更新导致历史事件被误当新事件

- 哪些信息无法从通知确定
  - 是否绝对真实完成了扣款（除非到账单/后续凭据可核对）
  - 真实完整订单号、完整银行卡号、用户敏感身份信息是否在所有场景可得
  - 全局“无漏记、无多记”证明
  - 退款是否完全归属某一支付（无可稳定关联字段时不应猜测）

- 系统绝不能承诺什么
  - 不承诺“仅凭通知即可实现零漏记、零多记”
  - 不承诺“每笔通知都一定能立刻形成正式账目”
  - 不承诺“通知状态与支付方官方账务状态实时一致”
  - 不承诺“未授权/未收到/异常通知也能安全推断交易不存在”

## 2) 交易状态定义

> 通用规则：**证据不足时必须进入待确认，不得猜测为成功或退款状态。**

- `DETECTED`
  - 收到什么证据：`notification event` 已经被监听、入库，至少包含来源、时间、摘要字段
  - 为什么不能算成功：尚未满足金额/状态/交易关系/重复判定条件，无法唯一标识交易对象与业务语义
  - 后续处理：进入规则匹配；失败或缺字段直接流转到“待确认”

- `SUCCESS`
  - 进入条件：
    - 同渠道模板命中成功语义
    - 有效金额、商户、时间、交易关系可满足一致性校验
    - 在重复识别规则下与已有交易可做可解释关联（或新交易新对象）
  - 后续处理：
    - 标记为“自动确认（未对账）”
    - 进入主账目候选链路，仍可被待确认/回滚规则覆盖

- `PARTIALLY_REFUNDED`
  - 进入条件：
    - 已存在一笔 `SUCCESS` 交易
    - 识别到退款事件并成功关联到该交易
    - 累计退款额 `0 < 退款累计 < 原支付金额`
  - 计算方式：
    - `累计退款 = Σ(所有已关联退款金额)`
    - `净支出 = 原支付金额 - 累计退款`
  - 后续处理：
    - 状态维持为 `PARTIALLY_REFUNDED`
    - 每次新增退款后重算 `累计退款/净支出`
    - 达到原金额边界时切换为 `FULLY_REFUNDED`

- `FULLY_REFUNDED`
  - 进入条件：
    - 已有原交易
    - 累计退款额 `==` 原支付金额（允许小额四舍五入/时区归一后比较）
  - 后续处理：
    - 将该交易归入已完全抵消状态
    - 净支出=0，保留原交易与退款链路

- `REVERSED`
  - 与正常退款/支付失败区别：
    - `REVERSED`：对已形成候选/成功态的交易进行人工或规则否定（误识别、重复疑似误归类、上下文冲突）
    - `支付失败`：若从通知可明确区分失败语义，则应进入待确认且不落为成功交易
  - 进入条件：
    - 人工判定误记账
    - 或证据冲突且无法可靠消歧（例如重复且来源冲突、金额与金额组合无法解释）
  - 后续处理：
    - 停止向正式账目自动确认
    - 保留审计与原证据链，不覆盖源事件内容

## 3) 数据可信度等级

- 自动确认
  - 定义：通知证据满足已验证规则（字段完整、状态语义清晰、重复关联可解释）
  - 条件：未与官方账单核对前也可进入，但可为用户展示“待对账”风险

- 待确认
  - 定义：金额、状态、商户、交易关系任一存在歧义，或证据不足
  - 条件：无法稳定关联、字段缺失、重复可能性高、状态冲突
  - 处理：固定停顿在待确认，不得转为已对账或支付成功

- 已对账
  - 定义：与用户导入的支付平台/银行账单成功匹配（T14 及以后）
  - 条件：交易身份、金额、时间窗、状态一致

- 关键约定
  - **自动确认不等于已对账。**

## 4) 隐私和日志规则

- 通知数据只在本地保存；默认不向任何服务器上传通知原文
- 默认不在 Logcat 输出完整通知正文、完整订单号、完整账号信息
- 测试样例必须脱敏，且与生产样例分离
- 敏感样例、真实账单、数据库文件不得提交 Git
- 后续远端同步必须单独设计：独立加密方案 + 用户授权 + 同步范围说明，不在本任务内实现

## T00 可执行验收检查（命令）

```bash
cd /home/hulk/androidapp/pills_app
git --git-dir=.git-repo --work-tree=. diff --check
git --git-dir=.git-repo --work-tree=. status --short
git --git-dir=.git-repo --work-tree=. log -1 --stat
```

预期结果：
- `diff --check` 无格式错误
- `status --short` 无输出
- 最新提交仅涉及 T00 设计文档
- 最新提交为一次边界清晰的提交，且不包含 Android 代码

## 最关键人工验收题（答案应唯一且可检索）

1. 收到一条“支付提醒”是否一定创建正式账目？
   - 不是。至少要进入 `DETECTED` 并通过成功条件后才能形成候选，证据不足必须是“待确认”。

2. 没有收到通知是否能证明没有发生交易？
   - 不能。通知缺失不代表无交易，最多代表“当前通道证据不足”。

3. 什么证据允许把交易标记为成功？
   - 成功语义文本 + 可解释金额 + 商户 + 时间 + 可稳定交易关系，且通过重复/冲突判定。

4. 同一支付被微信和银行各通知一次时如何处理？
   - 先做事件归并/关联：若可稳定关联到同一交易，作为同一交易的多来源事件合并；若冲突则进入待确认。

5. 退款找不到原支付时怎么办？
   - 不可自动生成负向普通消费，不可猜测归属，必须进入待确认并展示异常待处理。

6. 部分退款和多次退款如何计算？
   - 累计退款逐次相加形成退款累计；小于原额为 `PARTIALLY_REFUNDED`，等于原额则 `FULLY_REFUNDED`。

7. 通知和正式账单不一致时相信谁？
   - 以正式账单为更高优先级依据；若账单未导入则不做“对账确认”。

8. 用户如何知道自动采集可能已经失效？
   - 通过状态提示、来源完整性检查和权限/通道完整性告警（数据边界与不完整风险展示）。

9. 哪些隐私数据禁止写日志或提交 Git？
   - 通知原文全文、完整订单号、完整账号、敏感样例、真实账单、数据库文件。

10. 产品是否承诺仅凭通知实现绝对零漏记、零多记？
   - 不承诺。T00 明确拒绝该承诺并写入“阻断条件”。

若以上 10 项均可给出唯一回答，可判定 T00 通过，进入下一阶段（T01）前提成立。
